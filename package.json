{
  "name": "jproxy",
  "author": "Mark Smith-Guerrero <msmithgu@gmail.com>",
  "version": "0.0.1",
  "dependencies": {
    "jasmine-node"      : "1.0.26",
    "express"           : "3.0.x",
    "qs"                : "0.5.x",
    "socket.io"         : "0.9.x",
    "socket.io-client"  : "0.9.x"
  },
  "devDependencies": {
    "coffee-script": "~1.3.3"
  },
  "scripts": {
    "clean": "rm -rf node_modules",
    "prestart": "jasmine-node --coffee --forceexit spec",
    "start": "supervisor -e coffee -w 'lib,spec' -n error -n exit -x coffee -- server.coffee",
    "test": "jasmine-node --coffee --forceexit spec"
  }
}
